#include<stdio.h>
int main()
{
    int x;
    printf("Enter a number:");
    scanf("%d", &x);
    if (x>0)
    {
        printf("Posative number"); // jodi if ar por akta line thake tahola {} na dila hobe
    }
    else
    {
        printf("Non posative number"); // jodi else ar por akta line thake tahola {} na dila hobe
    }
}
