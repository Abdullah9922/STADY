#include<bits/stdc++.h>
using namespace std;

int main()
{
    int a,b,c,d,e,f,g,h;
    cin >> a >> b >> c >> d >> e >> f >> g >> h;

    if(a >= 60 && b >= 60 && c >= 60 && d >= 60 && e >= 60 && f >= 60 && g >= 60 && h >= 60  )
    cout<< "Eligible" << endl;
    else cout<< "Not Eligible" << endl;

}