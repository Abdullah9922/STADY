//https://judge.beecrowd.com/en/problems/view/1129
#include<stdio.h>
int main()
{
    int n,a;

    do{
        scanf("%d",&n);
        for(int i=1;i<=n;i++){  // for line.
            for(int j=1;j<=5;j++){  // for number in line.
                scanf("%d",&a);
                if()
            }

        }
    }
    while(n!=0);
}
